# Rust
/target
**/*.rs.bk
Cargo.lock.bak

# Frontend (Leptos/Trunk) build output
/crates/frontend/dist
/crates/frontend/pkg

# Local env / secrets
.env
*.local

# Runtime state (may contain provider API keys)
/state.json

# Legacy JS runtime leftovers (archived app lives in /legacy)
/node_modules
/.next
/legacy/node_modules
/legacy/.next
